# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'TestiOSCurrency' do
  use_frameworks!

  pod 'Swinject'
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'RxAlamofire'
  pod 'Reusable'
  pod 'ObjectMapper'
  pod 'Kingfisher'

  # Pods for TestiOSCurrency

  target 'TestiOSCurrencyTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
